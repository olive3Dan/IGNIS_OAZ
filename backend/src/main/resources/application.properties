# Database
spring.datasource.url=jdbc:postgresql://localhost:5432/ignis_oaz
spring.datasource.username=ignis
spring.datasource.password=ignis123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.spatial.dialect.postgis.PostgisPG10Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Server
server.port=8080

# Security - disable default for now (will configure JWT later)
spring.security.user.name=admin
spring.security.user.password=admin123
